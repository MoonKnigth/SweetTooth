meta {
  name: 08. Get User Orders
  type: http
  seq: 8
}
get {
  url: {{base_url}}/api/user/orders
  body: none
  auth: bearer
}
auth:bearer {
  token: {{token}}
}
headers {
  Accept: application/json
}
tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
  test("Returns success", function() {
    expect(res.body.status).to.equal('success');
  });
}
