meta {
  name: 15. Delete Category
  type: http
  seq: 15
}
delete {
  url: {{base_url}}/api/admin/categories/{{test_category_id}}
  body: none
  auth: bearer
}
auth:bearer {
  token: {{token}}
}
headers {
  Accept: application/json
}
tests {
  test("Status is 200 or 204", function() {
    expect([200, 204]).to.include(res.status);
  });
}
