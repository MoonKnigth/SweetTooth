meta {
  name: 17. Update Order Status
  type: http
  seq: 17
}
put {
  url: {{base_url}}/api/admin/orders/{{test_order_id}}/status
  body: json
  auth: bearer
}
auth:bearer {
  token: {{token}}
}
headers {
  Accept: application/json
}
body:json {
  {
    "status": "completed"
  }
}
tests {
  test("Status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
